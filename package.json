{
  "name": "pgp-functions",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "stripe": "^8.167.0",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.1.1"
  },
  "engines": {
    "node": "12"
  }
}
